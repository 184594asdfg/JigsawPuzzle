# 拼图音效

将两个语音文件放在本目录，并使用下列文件名（推荐 mp3）：

| 文件 | 用途 |
|------|------|
| `move.mp3` | 手指按下并开始拖动方块时播放 |
| `swap.mp3` | 松手落位成功且挤开了其他块（发生换位）时播放 |

代码中的引用路径为 `/audio/move.mp3`、`/audio/swap.mp3`（与 `/images/photo1.jpg` 同级写法）。

微信小程序常见格式：mp3、aac、m4a、wav。单文件建议控制在 200KB 以内以便打包。
